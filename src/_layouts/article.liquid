{%- layout 'default' -%}
{%- block 'main' -%}
  {%- include 'footer'
    date: date
    location: location
    canonical: canonical
    sources: sources
  -%}
  {%- include 'header'
    title: title
    summary: false
    vocab: vocab
  -%}
  {%- include 'content' with content -%}
  {%- include 'categories' with category -%}
  {%- include 'section-remarks' with remarks
    id: 'remarks'
    title: 'Remarks'
  -%}
  {%- include 'section-remarks' with webmentions
    id: 'webmentions'
    title: 'Webmentions'
  -%}
  {%- for this in collection -%}
    {%- assign related = collections.collection | where: 'fileSlug',this | first -%}
    {%- capture section_title %}More from the <a href="{{ related.url }}">{{ related.data.title }}</a> collection{%- endcapture -%}
    {%- assign articles = collections.article | includes: 'data.collection', this | excludes: 'url',page.url | reverse -%}
    {%- include 'section' with articles
      id: 'related'
      title: section_title
      scrollable: true
      items_limit: 6
      grid_size: '15rem'
      component: 'card'
    -%}
  {%- endfor -%}
{%- endblock -%}
